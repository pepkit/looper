colorama>=0.3.9
divvy>=0.5.0
eido>=0.2.1
jinja2
logmuse>=0.2.0
pandas>=2.0.2
pephubclient>=0.1.2
peppy>=0.40.0
pipestat>=0.6.0
pyyaml>=3.12
rich>=9.10.0
ubiquerg>=0.5.2
# This is a temporary hack: use a Yacman branch that makes Yacman's
# YAMLConfigmanager not capture SIGTERM / SIGKILL (and thus Ctrl+c events).
# In the near future, this will be replaced by a new version of Yacman that
# supports a read-only mode.
# git+https://github.com/databio/yacman.git@tweag/thread-unsafe
../yacman/
pydantic-argparse>=0.8.0
fastapi>=0.109.0
uvicorn>=0.26.0
