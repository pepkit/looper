pep_config: ./project_config_pipestat.yaml # pephub registry path or local path
output_dir: output
pipeline_interfaces:
  sample:  ./pipeline_interface1_sample.yaml
  #project: ./pipeline_interface_project.yaml
looper:
  all:
    output_dir: output

sample_modifiers:
  append:
    attr: "val"
  derive:
    attributes: [read1, read2]
    sources:
      SRA_1: "{SRR}_1.fastq.gz"
      SRA_2: "{SRR}_2.fastq.gz"
pipestat:
  project:
    project_name: test
    sample_name: frog_1
    schema_path: pipestat_output_schema.yaml
    results_file_path: tmp_pipestat_results.yaml #this absolute path for now
    #flag_file_dir: ../output/results_pipeline
  sample:
    project_name: test
    sample_name: frog_1
    schema_path: pipestat_output_schema.yaml
    results_file_path: tmp_pipestat_results.yaml #this absolute path for now
    #flag_file_dir: ../output/results_pipeline
